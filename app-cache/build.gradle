plugins {
}

dependencies {
}

jib {
    from {
        image = 'openjdk:21'
    }
    to {
        image = "kai1114/springkube-cache"
        tags = ['latest', '0.0.3']
    }
    container {
        creationTime = 'USE_CURRENT_TIMESTAMP'
    }
}
