plugins {
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
}

jib {
    from {
        image = 'openjdk:21'
    }
    to {
        image = "kai1114/springkube-cache"
        tags = ['latest', '0.0.4']
    }
    container {
        creationTime = 'USE_CURRENT_TIMESTAMP'
    }
}
